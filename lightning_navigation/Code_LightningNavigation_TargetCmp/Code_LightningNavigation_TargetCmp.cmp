<aura:component implements="lightning:isUrlAddressable">

    <aura:attribute name="firstName" type="String" default="Not Shashank" />
    <aura:handler name="init" value="{! this }" action="{! c.doInit }" />

    <h3>Hello {! v.firstName }</h3>

</aura:component>